  # BiasLens App Overview

  This is a code bundle for BiasLens App Overview. The original project is available at https://www.figma.com/design/Y5AoVEVfNINFrjbeczaHzS/BiasLens-App-Overview.

  ## Running the code

  Run `npm install` to install the dependencies.

  Run `npm run dev` to start the Next.js development server (default: http://localhost:3000).
  